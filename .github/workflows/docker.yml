name: Docker Image CI
on: 
  push:
    branches: ["main"]
  pull_request: 
    branches: ["main"]
jobs:
  #here docker is the job and github assign seperate server for each job to run the steps
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Build Backend Image
        run: |
          docker build \
          -f apps/docker/backend/Dockerfile \
          -t atiyadocker/django-react-backend:${{ github.sha }} \
          -t atiyadocker/django-react-backend:latest \
          .
      - name: Build Frontend Image
        run: |
          docker build \
          -f apps/docker/frontend/Dockerfile \
          -t atiyadocker/django-react-frontend:${{ github.sha }} \
          -t atiyadocker/django-react-frontend:latest \
          .
      - name: Build Nginx Image
        run: |
          docker build \
          -f apps/docker/nginx/Dockerfile \
          -t atiyadocker/django-react-nginx:${{ github.sha }} \
          -t atiyadocker/django-react-nginx:latest \
          .
      - name: push backend image
        run: |
          docker push atiyadocker/django-react-backend:${{ github.sha }}
          docker push atiyadocker/django-react-backend:latest
      - name: push frontend image
        run: |
          docker push atiyadocker/django-react-frontend:${{ github.sha }}
          docker push atiyadocker/django-react-frontend:latest
      - name: push nginx image
        run: |
          docker push atiyadocker/django-react-nginx:${{ github.sha }}
          docker push atiyadocker/django-react-nginx:latest
        

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: docker
  #   steps:
  #     - name: Deploy to server
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #           host: ${{ secrets.SERVER_HOST }}
  #           username: ${{secrets.SERVER_USERNAME }}
  #           password: ${{secrets.SERVER_PASSWORD }}

  #           script: |
  #            cd "C:\Users\Maaz\Desktop\deploy"
  #            docker compose pull
  #            docker compose up -d --remove-orphans
          
       
     
      
      
